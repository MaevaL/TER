{% set sidebar_active = 'admin' %}
{% set sidebar_active_sub_menu = 'users' %}

{% extends "::base.html.twig" %}

{% block body %}
    <div class="showback">

        <div class="row">
            <div class="col-lg-12">
                <h3><i class="fa fa-angle-right"></i> Edition du mot de passe d'un utilisateur : {{ user.firstname }} {{ user.lastname }}</h3>
            </div>
        </div>

        <br>
        <div class="row">
            <div class="col-lg-12">
                <a class="btn btn-theme-amu" href="{{ path('user_index') }}"><i class="fa fa-caret-left" aria-hidden="true"></i> Retour à la liste des utilisateurs</a>
            </div>
        </div>
        <br>

        <div class="row">
            <div class="col-lg-12">
                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <button class="btn btn-success" type="submit"><i class="fa fa-check" aria-hidden="true"></i> Editer</button>
                {{ form_end(edit_form) }}
            </div>
        </div>
    </div>
{% endblock %}

