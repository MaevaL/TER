{% set sidebar_active = 'admin' %}
{% set sidebar_active_sub_menu = 'users' %}

{% extends "::base.html.twig" %}

{% block body %}
    <div class="showback">
        <div class="row">
            <div class="col-lg-12">
                <h3><i class="fa fa-angle-right"></i> Importer un fichier d'enseignants</h3>
            </div>
        </div>

        <br>
        <div class="row">
            <div class="col-lg-12">
                <a class="btn btn-theme-amu" href="{{ path('user_index') }}"><i class="fa fa-caret-left" aria-hidden="true"></i> Retour à la gestion des utilisateurs</a>
            </div>
        </div>

        <br>

        <div class="row">
            <div class="col-lg-12">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-success" type="submit"><i class="fa fa-check" aria-hidden="true"></i> Importer</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}