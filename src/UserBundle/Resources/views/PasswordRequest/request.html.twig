{% extends "::base_offline.html.twig" %}

{% block offline_title %}Mot de passe oublié ?{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="form-group text-center">
                <button class="btn btn-primary" type="submit">Demander mon mot de passe</button>
            </div>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}